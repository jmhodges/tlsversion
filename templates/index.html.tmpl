{{ define "title" }}{{ .Version }} — tlsversion.com{{ end }}
{{ define "content" -}}
<p class="kicker">Your connection negotiated</p>
<p class="version">{{ .Version }}</p>
<p class="note">That's the protocol your browser and this server agreed on to encrypt the page you're reading. <a href="/about">What does that mean?</a></p>
{{- end }}
